{
  "schema_version": 1,
  "lesson_number": 24,
  "question_id": 24,
  "mode": "inspect",
  "project_impact": "none",
  "title": "Understand static variables through LanguageLab state",
  "info_language": "te-Latn",
  "answer": "A static variable belongs to the class itself rather than to each individual object instance. There is one class-level copy associated with the loaded class, and all instances access that shared state. Static fields are commonly used for constants, counters, caches, configuration shared at the class level, or other data that logically belongs to the type rather than one object. In `LanguageLab`, `private static int batches` is shared across all `LanguageLab` instances, while `accepted` is an instance field with a separate value per object. The `VALID` and `GROUND` fields are `public static final` constants. Static mutable state should be used carefully because shared access can create concurrency and test-isolation concerns.",
  "steps": [
    {
      "title": "Open the class containing static and instance fields",
      "question": "Compare the static constants and batches counter with the non-static accepted field. The existing `LanguageLab` deliberately places class-level fields and an instance field beside each other, making it a better teaching example than a separate synthetic class. Which declarations should you compare to distinguish shared static state from per-object state?",
      "why": "Compare the static constants and batches counter with the non-static accepted field.",
      "why_te": "`LanguageLab` lo `VALID`, `GROUND`, `batches` static fields; `accepted` instance field. Same class lo side-by-side compare chesthe class-level shared state mariyu object-specific state difference clear ga kanipistundi.",
      "software": "intellij",
      "required_capability": "open-file",
      "feature_available": true,
      "action": {
        "action": "openFile",
        "data": {
          "file": "src/main/java/com/aerotopo/learning/LanguageLab.java"
        }
      }
    },
    {
      "title": "Inspect static final constants",
      "question": "Static avoids one copy per instance, while final prevents the constant from being reassigned. The `VALID` and `GROUND` flags are declared `public static final`, combining class-level ownership with immutability after initialization. Why are constants commonly modeled this way when every `LanguageLab` object should refer to the same fixed bit values?",
      "why": "Static avoids one copy per instance, while final prevents the constant from being reassigned.",
      "why_te": "`static` valla constants class ki belong avutayi, prathi object ki separate copy avasaram ledu. `final` valla initialization taruvata values reassign cheyyaleru; flags stable ga untayi.",
      "software": "intellij",
      "required_capability": "highlight-target",
      "feature_available": true,
      "action": {
        "action": "highlightTarget",
        "data": {
          "target": {
            "type": "line",
            "file": "src/main/java/com/aerotopo/learning/LanguageLab.java",
            "line": 7
          }
        }
      }
    },
    {
      "title": "Inspect the shared batches counter",
      "question": "An instance field would give each LanguageLab object its own independent batches value rather than one shared counter. Unlike the constants, `batches` is mutable but still declared static, so every instance observes and updates the same class-level counter. What would be different if this field were declared as a normal instance variable instead?",
      "why": "An instance field would give each LanguageLab object its own independent batches value rather than one shared counter.",
      "why_te": "`batches` static kabatti all LanguageLab instances same counter ni share chestayi. Static remove chesthe prathi object ki own batches value untundi, global class-level count maintain avvadu.",
      "software": "intellij",
      "required_capability": "highlight-target",
      "feature_available": true,
      "action": {
        "action": "highlightTarget",
        "data": {
          "target": {
            "type": "line",
            "file": "src/main/java/com/aerotopo/learning/LanguageLab.java",
            "line": 9
          }
        }
      }
    },
    {
      "title": "Compare the accepted instance field",
      "question": "The declarations differ primarily in static ownership, making the storage semantics easy to contrast. The neighboring `accepted` field is deliberately non-static, so its value belongs to a particular `LanguageLab` object and can differ between instances. Why does comparing lines 9 and 10 make the class-versus-instance ownership distinction especially clear?",
      "why": "The declarations differ primarily in static ownership, making the storage semantics easy to contrast.",
      "why_te": "`batches` class-level shared field, `accepted` object-level field. Adjacent declarations ni compare chesthe `static` keyword ownership/storage semantics meeda exact effect easy ga understand avvutundi.",
      "software": "intellij",
      "required_capability": "highlight-target",
      "feature_available": true,
      "action": {
        "action": "highlightTarget",
        "data": {
          "target": {
            "type": "line",
            "file": "src/main/java/com/aerotopo/learning/LanguageLab.java",
            "line": 10
          }
        }
      }
    },
    {
      "title": "Inspect static initialization",
      "question": "Static initialization occurs as part of class initialization and establishes shared state for the class. The static initializer assigns `batches = 0` when the class is initialized rather than each time a new `LanguageLab` instance is constructed. What does this initialization point tell you about the lifecycle of class-level static state?",
      "why": "Static initialization occurs as part of class initialization and establishes shared state for the class.",
      "why_te": "Static initializer class load/initialize stage lo once run ayi shared `batches` state ni initialize chestundi. Prathi new object creation appudu separate ga execute ayye instance initialization kaadu.",
      "software": "intellij",
      "required_capability": "highlight-target",
      "feature_available": true,
      "action": {
        "action": "highlightTarget",
        "data": {
          "target": {
            "type": "line",
            "file": "src/main/java/com/aerotopo/learning/LanguageLab.java",
            "line": 11
          }
        }
      }
    },
    {
      "title": "Inspect mutation of the shared counter",
      "question": "Every call increments the same class-level value, so the counter accumulates across instances. Inside `classify`, the statement `batches++` updates the static counter without referring to any particular object's copy because no such per-instance copy exists. How would calls through multiple `LanguageLab` instances affect this single field over time?",
      "why": "Every call increments the same class-level value, so the counter accumulates across instances.",
      "why_te": "Different LanguageLab objects nundi `classify` call chesina kuda `batches++` same static field ni update chestundi. Counter instances across shared ga accumulate avutundi.",
      "software": "intellij",
      "required_capability": "highlight-target",
      "feature_available": true,
      "action": {
        "action": "highlightTarget",
        "data": {
          "target": {
            "type": "line",
            "file": "src/main/java/com/aerotopo/learning/LanguageLab.java",
            "line": 15
          }
        }
      }
    },
    {
      "title": "Inspect where batches is returned",
      "question": "The snapshot captures a class-wide counter and object-specific count together, making their different ownership visible in one result. The `Snapshot` returned by `classify` includes the current `batches` value, exposing shared class state alongside the object's `accepted` count. Why is this return value a useful way to observe static and instance state participating in the same operation?",
      "why": "The snapshot captures a class-wide counter and object-specific count together, making their different ownership visible in one result.",
      "why_te": "`Snapshot` lo shared `batches` mariyu instance-specific `accepted` rendu capture avutayi. Same operation result lo different ownership models practical ga compare cheyyachu.",
      "software": "intellij",
      "required_capability": "highlight-target",
      "feature_available": true,
      "action": {
        "action": "highlightTarget",
        "data": {
          "target": {
            "type": "line",
            "file": "src/main/java/com/aerotopo/learning/LanguageLab.java",
            "line": 26
          }
        }
      }
    },
    {
      "title": "Review all usages of the static field",
      "question": "A static variable can become risky when many parts of a codebase mutate shared state, so developers often inspect every usage before changing it. What should the Find Usages view reveal about how narrowly `batches` is currently initialized, incremented, and exposed?",
      "why": "The usages should show the declaration, static initialization, increment, and snapshot return path in the same class.",
      "why_te": "Find Usages `batches` declaration, static initializer, increment, snapshot return locations ni show chestundi. Shared mutable state scope small ga unda leda widespread ga unda ani developer quickly assess cheyyachu.",
      "software": "intellij",
      "required_capability": "find-usages",
      "feature_available": true,
      "action": {
        "action": "findUsages",
        "data": {
          "symbol": "batches",
          "file": "src/main/java/com/aerotopo/learning/LanguageLab.java",
          "results": [
            {
              "file": "src/main/java/com/aerotopo/learning/LanguageLab.java",
              "line": 9,
              "preview": "private static int batches;"
            },
            {
              "file": "src/main/java/com/aerotopo/learning/LanguageLab.java",
              "line": 11,
              "preview": "static { batches = 0; }"
            },
            {
              "file": "src/main/java/com/aerotopo/learning/LanguageLab.java",
              "line": 15,
              "preview": "batches++;"
            },
            {
              "file": "src/main/java/com/aerotopo/learning/LanguageLab.java",
              "line": 26,
              "preview": "return new Snapshot(flags & VALID, accepted, batches, label);"
            }
          ]
        }
      }
    }
  ]
}
